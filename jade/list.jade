doctype html
html(lang="en")
  head
    link(rel='stylesheet', href='/css/main.css')   
    title= BlogList
  body
    h1 Comments
  table
      each val, i in message
        li
          h3 #{val.title}
          h4 #{val.user}
